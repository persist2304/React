import { ArticleData } from "../constants/todoLists"

const ArticleReducer = (state = ArticleData, action) => {
    switch(action.type){
        default:
            return state
    }
}

export default ArticleReducer;