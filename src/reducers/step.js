import { StepData } from "../constants/todoLists"

const StepReducer = (state = StepData, action) => {
    switch(action.type){
        default:
            return state
    }
}

export default StepReducer;